Има ли смисъл да използваме наши версии на библиотечни функции?
    Библиотеките имат ли отрицателен ефект?

Има ли значение дали ще подадем на функция като параметър char[] или *char ?
    function(char[]);
    function(*char);
        Има ли конвенция?